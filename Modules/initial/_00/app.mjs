

const sum = (x, y) => { console.log(x + y) }

const sub = (x, y) => { console.log(x - y) };

const mult = (x, y) => { console.log(x * y) };

const div = (x, y) => { console.log(x / y) };

const pow = (x, y) => { console.log(x ** y) };

const sqrt = (x) => { console.log(Math.sqrt(x)) };



export {
  sum,
  sub,
  mult,
  div,
  pow,
  sqrt,
};

















